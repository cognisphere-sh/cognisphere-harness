# route-chats — changelog

## 1.0.0

- Initial version: the `--chat` regex forms, parking a group chat in its own
  thread, folding several chats into one shared thread (and what that shares),
  listing/removing rules, and `/reset` semantics in a routed thread.
