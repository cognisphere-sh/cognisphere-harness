# route-email — changelog

## 1.0.0

- Initial version: the default `<Subject>[<gmailThreadId>]` thread id, the
  three match patterns and which to prefer, the send-then-route recipe for
  keeping a reply in the thread it was sent from, listing/removing rules, and
  the limits of a rule (delivery target only, one poll interval to take
  effect).
