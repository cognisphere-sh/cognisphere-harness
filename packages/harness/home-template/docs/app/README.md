# App documentation

Documentation for the user-facing app (`app/`). Owned by whoever changes the
code — the developer agent or a human — and updated **after every
change** under `app/` (see `CLAUDE.md` §7).

Document here: the app's structure, routes/pages, how it reaches the harness
(`HARNESS_URL`, proxying — see `app/README.md` for the deploy contract), and
any environment it needs.

_No app built yet — start this doc with the app._
