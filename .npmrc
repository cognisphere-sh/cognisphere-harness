# Flat node_modules layout (npm-like) so configs that resolve transitive deps
# keep working. Still uses pnpm's content-addressed store + hardlinks, so the
# per-harness disk-dedup benefit is preserved.
node-linker=hoisted
